{% extends 'base.html.twig' %}

{% block title %}Votre recherche pour la ville de  {% if cityInfo %}{{ cityInfo[0].nom }}{% endif %}!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    {% if cityInfo %}
    <p>Votre recherche pour la ville de  {{ cityInfo[0].nom }} à bien était effectuer✅</p>
    <h1>Voici se que nous avont trouver à propos de  {{ cityInfo[0].nom }}!</h1>

    <section class="content">
        <p>La ville de {{ cityInfo[0].nom }} à une population de <strong>{{ cityInfo[0].population }}</strong> personnes et est située dans le département numéro <strong>{{ cityInfo[0].codeDepartement }}</strong> </p>
    </section>
        {% if cityEtab.features %}
            <section>
                <h2>Voci quelques infos que nous avons trouver pour {{ cityEtab.features[0].properties.nom }}</h2>
                <ul>
                    <li>rue : {{ cityEtab.features[0].properties.adresses[0].lignes[0] }}</li>
                    <li>site web: <a href="{{ cityEtab.features[0].properties.url }}" target="_blank">{{ cityEtab.features[0].properties.url }}</a></li>
                </ul>
            </section>
        {% else %}
            <p>Nous n'avons malheuresement pas assez d'information sur la mairie de <b>{{ cityInfo[0].nom }}</b></p>
        {% endif %}
    {% else %}
        <p>Votre recherche est peut-être mal formulé</p>
    {% endif %}
</div>
{% endblock %}
